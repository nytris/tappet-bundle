framework:
    secret: 'test-secret'
    test: true
    router:
        resource: '%kernel.project_dir%/config/routes.yaml'
        utf8: true

services:
    logger:
        class: Psr\Log\NullLogger

    Tappet\Bundle\Tests\Functional\Util\Fixture\TestFixtureLoader:
        autoconfigure: true
